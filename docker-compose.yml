version: '2'

services:
  api:
    build: .
    command: npm run dev
    ports:
      - "8000:8000"
    volumes:
      - .:/code
      - api-deps:/code/node_modules
    environment:
      - VIRTUAL_HOST=davidpellerin.dev

volumes:
  api-deps: